#include "VectorRotationUtil.hpp"

namespace space::util {
    Matrix VectorRotationUtil::rotateM1(const Vector& rotationVec, float angle) {
        return {4, 4, {{}, {}, {}, {}}};
    }

    Matrix VectorRotationUtil::rotateM2(const Vector& rotationVec, float angle) {
        return {4, 4, {{}, {}, {}, {}}};
    }

    Matrix VectorRotationUtil::rotateM3(const Vector& rotationVec, float angle) {
        return {4, 4, {{}, {}, {}, {}}};
    }

    Matrix VectorRotationUtil::rotateM4(const Vector& rotationVec, float angle) {
        return {4, 4, {{}, {}, {}, {}}};
    }

    Matrix VectorRotationUtil::rotateM5(const Vector& rotationVec, float angle) {
        return {4, 4, {{}, {}, {}, {}}};
    }
} // namespace space::util